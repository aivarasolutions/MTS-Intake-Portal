# Database Configuration
# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/mts1040?schema=public"

# Individual PostgreSQL connection parameters (optional, used by some tools)
PGHOST="localhost"
PGPORT="5432"
PGUSER="user"
PGPASSWORD="password"
PGDATABASE="mts1040"

# Session Configuration
# Secret key for signing session cookies (generate a secure random string)
SESSION_SECRET="your-super-secret-session-key-change-in-production"

# Application Configuration
NODE_ENV="development"
