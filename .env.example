# Database Configuration
# PostgreSQL connection string (REQUIRED)
# This is the primary connection method - individual PG* variables below are optional
DATABASE_URL="postgresql://user:password@localhost:5432/mts1040?schema=public"

# Individual PostgreSQL connection parameters (OPTIONAL - only for CLI tools like psql)
# NOTE: The application uses DATABASE_URL only. These are for development tools.
PGHOST="localhost"
PGPORT="5432"
PGUSER="user"
PGPASSWORD="password"
PGDATABASE="mts1040"

# Security Configuration
# Secret key for signing session cookies (REQUIRED)
# Generate: openssl rand -base64 32
SESSION_SECRET="your-super-secret-session-key-change-in-production"

# Data encryption key for sensitive data (REQUIRED)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DATA_ENCRYPTION_KEY="your-64-character-hex-encryption-key-change-in-production"

# Application Configuration
NODE_ENV="development"
